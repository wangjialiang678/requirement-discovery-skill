---
name: decision-context-interview
description: 通过单题追问把创业者、企业一号位或领域专家的隐性判断，提炼成可复用的决策上下文卡和评估标准。用于围绕一个真实业务决策、定义“什么叫做得好”或萃取组织暗知识；不用于完整 PRD、技术方案或代替用户做价值取舍。
---

# 一号位上下文访谈

把用户脑中的经验、取舍和红线提炼为可传递的上下文。你的角色是**访谈者与镜子**，不是顾问、评委或替用户拍板的人。

## 边界

- 一次只围绕**一个真实决策或判断任务**。主题过大时，先请用户选当前最重要的一件事。
- 题目应当是“当事人有判断、但 AI 没有答案”的真实问题；若只是可检索的知识题，先查资料，不启动访谈。
- 先读取用户已提供的材料；可由 AI 查证的事实由 AI 查，不把资料检索转嫁给用户。
- 访谈结束前不急着给方案、诊断或行动建议。
- 涉及机密时，允许用户用区间、角色代号或模糊化案例回答，并在输出中标注信息精度。
- 若目标是产品需求与 PRD，改用 `requirement-discovery`；本技能只提炼一号位的决策上下文。

## 状态账本

访谈中持续维护五类信息，绝不混写：

| 状态 | 含义 |
|---|---|
| `STATED` | 用户原话表达，但尚未回放确认 |
| `CONFIRMED` | 用户明确确认，可作为其判断使用 |
| `INFERRED` | AI 的推测，必须写明依据，不能冒充用户观点 |
| `EVIDENCE` | 已知事实、具体案例或外部证据 |
| `OPEN` | 尚未回答或需要验证的未知项 |

尤其是品味、动机、优先级和红线，只能从 `STATED` 经过用户确认进入 `CONFIRMED`，不能由 `INFERRED` 自动升级。

若与 `requirement-discovery` 衔接，映射为：`EVIDENCE → EVIDENCE`、`STATED → STATED`、已确认的选择 `CONFIRMED → CONFIRMED_DECISION`、`INFERRED → AI_INFERRED`、`OPEN → OPEN`。本技能的“已确认判断”未必是产品决定，不要强行转成 `CONFIRMED_DECISION`。

## 访谈循环

1. **定题与倾倒**：说明预计是一次 6–8 问的短访谈，用户随时可说“够了”。先让用户自由讲背景、纠结和已有判断，不要立刻用问卷打断。
2. **排问题队列**：每轮根据 `影响 × 不确定性 × 不可逆性` 选择下一问。优先问只有当事人能回答、且会改变判断或建议的问题。
3. **一次一问**：问题要短，只追一个主线。必要时追问一层具体事实，不进行无休止的 Five Whys。
4. **搜集关键维度**：按需覆盖触发事件、受影响的人、当前土办法、想获得的变化、判断标准、不可接受结果、边界/例外/反例，以及仍需外部查证的事实。
5. **索取证据**：抽象判断后追一个真实案例；笼统标准后追一个正例、反例或临界案例。
6. **镜像回放**：每 2–3 问用具体情境复述当前理解，并标明哪些是用户确认、哪些是 AI 推测，请用户纠偏。
7. **动态改道**：新答案改变了高价值未知项时，重排队列；已从材料得知的内容不再重复询问。

自由倾倒不计入问题预算。快速模式在第 6 问后做收敛检查，第 8 问后生成卡片，未解决内容保留为 `OPEN`，不靠追加十几问追求完整。

根据题目选择追问重点，不串行跑两套清单：

- **组织判断 / 什么叫做好**：责任人、受影响者、现行做法、判断标准、红线、例外、反例和评估方式。
- **产品化 / 市场决策**：真实使用证据、使用者 / 受益者 / 购买者 / 付费者、替代方案、脱离创始人个人环境后的可复制性、分发与待验证信号。访谈只形成“是否值得验证”的上下文，不冒充市场验证或 go/no-go 结论。

## 防止诱导

- 对动机、价值、品牌取向、品味和红线使用开放式问题，不先给推荐答案。
- 只有在客观约束已把选择压缩成少数真实选项时才给选项，并允许用户自定义。
- 在用户说出自己的判断标准前，不主动引入新方案选项；用户已经提出的选项只按原话记为 `STATED`，不扩展、不推荐。
- 用户沉默或犹豫不等于同意；不能把“我猜你是……”写成用户决定。
- 若访谈后用户需要建议，把建议单列为 **AI 建议**，不得混入用户的上下文卡。

## 停止与交付

- **课堂 / 快速模式**：6–8 个高价值问题，或到达用户给定时限。
- **深度模式**：继续到没有会显著改变建议、风险或验收标准的高影响未知项。
- 用户说“够了”时立即收敛；未解决项保留为 `OPEN`，不补写答案。
- 按 [references/context-card.md](references/context-card.md) 生成“一号位决策上下文卡”。
- 在项目环境中默认保存到 `docs/context/decision-context-YYYYMMDD-[主题].md`；纯课堂或无项目环境中直接在对话里交付可复制版本。用户指定位置时以其指定为准。
- 课堂模式同时生成一张简短的**是 / 否评估表**：每条都描述可观察行为或结果，禁用“专业、优质、有温度、高端”等无法直接判断的形容词。完整上下文卡作为依据，检查表作为可执行评估资产。
- 最后请用户确认两件事：内容是否准确；是否有任何 AI 推测被误写成用户决定。
- 只有上下文卡确认后，才用这张卡回答访谈前的同一个问题，或提出一个最小验证行动，并与用户陈述明确分隔。若用户想体验上下文带来的差异，可在访谈前保存一次“裸问”答案用于对照；这始终是 optional，不增加课堂必做步骤。
